import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div style={{ margin: 48 }}>Examples will be here in the future</div>
  </React.StrictMode>
);
